// Predict and explain...
// What will happen when this program is run?
// Check your prediction and explanation by running the program.
// Fix anything that needs fixing.

const person = {
  name: "Jemima",
  location: "Glasgow",
  id_number: 9,
};

console.assert(person.name === "Jemima");
console.assert(person.location === "Glasgow");
console.assert(person.id_number === 9);
// It would give an error because in line 9 id_number has = symbol and has to be : and location doesn't have a value.
console.log(person.name === "Jemima");
console.log(person.location === "Glasgow");
console.log(person.id_number === 9);
